<Window x:Class="SatelliteTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SatelliteTracker"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="405" Width="800">
    
    <Grid Background="#1E1E1E" Margin="0,0,0,-6">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="140" />
        </Grid.RowDefinitions>

        <!-- 🗺️ MAP -->
        <Border Grid.Row="0" Grid.Column="0"
        Margin="5"
        Background="#2A2A2A"
        CornerRadius="20">

            <Grid>
                <wv2:WebView2 x:Name="MapView"
                      Margin="5"/>
            </Grid>
        </Border>

        <!-- 📋 RIGHT PANEL -->
        <Grid Grid.Row="0" Grid.Column="1" Margin="5">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- LIST -->
            <Border Background="#2A2A2A"
                    CornerRadius="20"
                    Margin="0,0,0,10">

                <DockPanel Margin="10">

                    <TextBlock Text="СПИСОК СПУТНИКОВ"
                               Foreground="White"
                               FontSize="16"
                               Margin="5"
                               DockPanel.Dock="Top"/>

                    <ListBox ItemsSource="{Binding Satellites}"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="White">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#3A3A3A"
                                        Margin="5"
                                        Padding="8"
                                        CornerRadius="10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="Bold"/>

                                        <Rectangle Width="20" Height="20"
                                                   Margin="0,5,0,0"
                                                   Fill="{Binding TrackColor}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                    </ListBox>

                </DockPanel>
            </Border>

            <!-- BUTTONS -->
            <StackPanel Grid.Row="1" VerticalAlignment="Bottom">

                <Button Content="ДОБАВИТЬ"
                        Margin="0,5"
                        Height="45"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"/>

                <Button Content="ИЗМЕНИТЬ"
                        Margin="0,5"
                        Height="45"
                        Background="#2196F3"
                        Foreground="White"
                        FontWeight="Bold"/>

                <Button Content="УДАЛИТЬ"
                        Margin="0,5"
                        Height="45"
                        Background="#F44336"
                        Foreground="White"
                        FontWeight="Bold"/>

            </StackPanel>

        </Grid>

        <!-- ➕ TLE PANEL -->
        <Border Grid.Row="1" Grid.Column="0"
                Margin="5"
                Background="#2A2A2A"
                CornerRadius="20">

            <Grid Margin="15">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Name"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <TextBox Grid.Column="1"
                         Margin="5"
                         Text="{Binding NewSatelliteName}"/>

                <TextBlock Text="TLE 1"
                           Grid.Row="1"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <TextBox Grid.Row="1" Grid.Column="1"
                         Margin="5"
                         Text="{Binding TLE1}"/>

                <TextBlock Text="TLE 2"
                           Grid.Row="2"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <TextBox Grid.Row="2" Grid.Column="1"
                         Margin="5"
                         Text="{Binding TLE2}"/>

                <Button Grid.Column="2"
                        Grid.RowSpan="3"
                        Margin="10"
                        Content="ADD"
                        Background="#4CAF50"
                        Foreground="White"/>

            </Grid>

        </Border>

    </Grid>
</Window>